---
showSupportUrl: false
showKnownIssuesUrl: false
showReleaseNotesUrl: false
showDonateUrl: false
requirements:
  requiredStorage: 32
  requiredRam: 2.0
  # Kept explicit so Calamares never falls back to its example URL. The
  # installer does not contact it unless "internet" is added to check below.
  internetCheckUrl: https://deb.debian.org/
  check: [storage, ram, power, root]
  required: [storage, ram, root]
